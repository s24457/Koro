#include <iostream>
using namespace std;

auto main (int argc, char** argv) -> int {
    auto a = stoi(argv[1]);
    auto b = stoi(argv[2]);

    for (int i = a; i < b; ++i) {
        cout << i << endl;
    }
    return 0;
}
