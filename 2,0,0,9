#include <iostream>
using namespace std;

auto main (int argc, char** argv) -> int {
    auto const a = stoi(argv[1]);
    auto f = 1, i = 1;

    while (i<=a){
        f*=i;
        i++;
    }
    cout << f << endl;
    return 0;
}
