#include <iostream>
using namespace std;

auto main (int argc, char** argv) -> int {
    auto const a = stoi(argv[1]);
    for (int i = 0; i <= a; ++i) {
        for (int j = 0; j < i; ++j) {
            cout << "*";
        }
        cout << endl;
    }
    return 0;
}
