#include <iostream>
using namespace std;

auto main (int argc, char** argv) -> int {
    auto const a = stoi(argv[1]);
    auto f = 1;
    for (int i = 1; i <= a; ++i) {
        f*=i;
    }
    cout << f << endl;
    return 0;
}
